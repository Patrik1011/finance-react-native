export enum Role {
  Admin = 'ADMIN',
  User = 'USER',
  PremiumUser = 'PREMIUM_USER',
}

export enum AccessType {
  Public = 'PUBLIC',
  User = 'USER',
  PremiumUser = 'PREMIUM_USER',
  Admin = 'ADMIN',
}
